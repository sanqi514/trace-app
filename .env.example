# LLM 代理（用于「优化表达」「AI 复盘」「每周总结」「摘要」，OpenAI 兼容接口）
# 这是「兜底」配置：当用户没有在 App 设置里自填模型/Key 时，服务端用这里的值。
# 若你希望每个用户都自带 Key，也可以全部留空。
LLM_API_KEY=sk-your-key-here
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# 说明：
# 1. 语音转写已改为浏览器自带 Web Speech API，本地实时进行，无需任何服务端转写 Key。
# 2. 用户可在 App「设置」中自填 模型 / 接口地址 / API Key，仅存于本机浏览器（IndexedDB），
#    经此代理转发、不直连厂商、不上传第三方；留空则回退到上面的服务端环境变量。

